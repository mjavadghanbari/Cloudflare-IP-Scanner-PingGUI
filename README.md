# Cloudflare-IP-Scanner-PingGUI
A lightweight Windows GUI tool for testing TCP latency and finding the best-performing Cloudflare IPs. Supports CIDR ranges, parallel scanning, CSV export, and clipboard copy.
